import { FilroCompletadoPipe } from './filro-completado.pipe';

describe('FilroCompletadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilroCompletadoPipe();
    expect(pipe).toBeTruthy();
  });
});
